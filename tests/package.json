{
  "name": "zolty-blog-site-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Dynamic site scanning: visual regression, link checking, security headers",
  "scripts": {
    "test": "playwright test",
    "test:visual": "playwright test visual",
    "test:links": "playwright test links",
    "test:security": "playwright test security",
    "update-snapshots": "playwright test visual --update-snapshots",
    "report": "playwright show-report playwright-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
